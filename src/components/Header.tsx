import React from "react";

const Header: React.FC = () => {
  return (
    <header className="p-3 sm:p-6 bg-black">
      <h1 className="font-bold">Kaleidoscoper</h1>
    </header>
  );
};

export default Header;
